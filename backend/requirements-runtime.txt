# Только RUNTIME-зависимости API (для слим docker-образа).
# БЕЗ: gensim / scikit-learn / matplotlib / pandas / openpyxl / nbformat / nbconvert /
#      ipykernel (это для обучения/ноутбука) и БЕЗ nvidia-cu12 (GPU-torch ставится отдельно, CPU).
# torch/torchvision ставятся отдельно с CPU-индекса (см. Dockerfile).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic-settings>=2.6
python-multipart>=0.0.12
httpx>=0.27
openai>=1.54
qdrant-client>=1.12
neo4j>=5.25
sqlalchemy[asyncio]>=2.0
asyncpg>=0.31
alembic>=1.18
sentence-transformers>=3.3
transformers>=5.12
faster-whisper>=1.0
easyocr>=1.7
yt-dlp>=2024.11
numpy>=2.4
